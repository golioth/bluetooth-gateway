sample:
  description: Use LightDB get
  name: lightdb-get
common:
  harness: pytest
  tags:
    - golioth
    - lightdb
    - socket
    - goliothd
tests:
  sample.pouch_gateway.golioth.lightdb_get:
    sysbuild: true
    platform_allow:
      - nrf52_bsim
